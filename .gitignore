# Build output
bin/
obj/
publish/
out/
msix-layout/
TestResults/

# SDK build tools (restored by pack.ps1 from NuGet)
tools/

# NuGet package caches leaked by container test runs
nuget/

# Signing material — private key and password must never be committed
cert/

# Local scratch
*.log

# AI/agent working artifacts — not part of the project
CLAUDE.md
.claude/
*.handoff.md
